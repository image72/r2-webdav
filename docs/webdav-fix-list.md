# WebDAV 缺陷修复列表

来源：2026-09-23 的 WebDAV 实现审计（RFC 4918/9110 语义 + 规模/并发/存储层）。
每一项都标注了证据来源：**[实测]** 为本地 `wrangler dev` 的原始结果，**[推导]** 为依据
Cloudflare 官方文档 + 代码运算量推算（本地无法复现平台侧限额）。

状态取值：

- `已修复` —— 已改代码、有实测或回归证据。
- `已缓解` —— 后果已消除（改为显式失败/收敛），但根本上限受平台限制，仍列入下方平台受限表。
- `未修复` —— 审计确认存在、且**本轮没有改**（当前列表里已经没有这类条目了）。
- `平台受限` —— 无法在 Workers 上按要求完成，记录后跳过。
- `记录` / `有意偏离，保留并记录` —— 刻意的设计取舍，不是待办。

> **状态列已于 2026-09-23 逐项复核**（上一个版本只更新了上面的汇总表，遗漏了逐项状态列，已改正）。
> 复核方式：对每个“以为已修”的条目重新发请求实测，而不是看代码印象。
> 复核又抓出两条"其实没修"的：**B6**（LOCK 的 5 项子检查仍全部不符，连 RFC 要求的 `<lockroot>` 也没有）
> 和 **B9**（目录 GET 无尾斜杠仍为 200，应 301）。这两条当时就不在任何修复批次里 ——
> **已于批次 7 修复**。

## 修复进展

| 批次 | 覆盖 ID                                                  | 状态                                                 |
| ---- | -------------------------------------------------------- | ---------------------------------------------------- |
| 1    | A1 A2 A5 A6 B1 B2 B7 C1 C2 C3 C4 C5 C7 C9 C10 C11 C14 D6 | **已修复**，回归 72/72 通过                          |
| 2    | A3 A4 B3                                                 | **已修复**，回归 40/40 通过                          |
| 3    | B10                                                      | **已修复**，回归 36/36 通过，且 B1/B2 套件重跑无回归 |
| 4    | B4 B5 B8 C6                                              | **已修复**，回归 43/43 通过                          |
| 5    | C12 C13 D2 D4 D7                                         | **已修复**，回归 24/24 通过                          |
| 6    | F1 F2 F3（网页界面：上传/新建同名保护）                  | **已修复**，Playwright 实测 6/6 + 4/4 通过           |
| 7    | B6 B9（LOCK/UNLOCK 协议语义；目录缺尾斜杠的 301）        | **已修复**，新增专项 59/59 通过                      |

**最终回归：274 项断言全部通过**（b1b 72 / b2 40 / b3 36 / b4 43 / b5 24 + 专项 b6 59），
另加"服务端实际输出的内联脚本用 `node --check` 校验"（`ui.ts` 改动后必做，
因为嵌入式客户端 JS 对 `tsc` 完全不可见）。

> **关于两条被改掉期望值的旧断言**：`b1b` 与 `b4` 里原本有 `reg LOCK → 200`、`reg UNLOCK → 204`，
> 发的是**裸请求**（无 body、无 header）。它们编码的正是 B6 的缺陷本身
> （裸 LOCK 也发 token 回 200、UNLOCK 不校验 token 一律 204），所以已按 RFC 4918
> §9.10.1（新建锁必须有 XML 请求体）与 §9.11.1（无 Lock-Token → 400）改成 400。
> 这是**修正测试对错误实现的编码**，不是放水；完整的 LOCK/UNLOCK 语义在新增的 `/tmp/b6.mjs`。

**部署记录**

| 版本 ID                                | 时间              | 内容                                                                |
| -------------------------------------- | ----------------- | ------------------------------------------------------------------- |
| `ae560814-f2da-4a9f-9d11-3c8721ab0f22` | 2026-09-23 03:03Z | **批次 7（B6/B9）上线**：LOCK/UNLOCK 协议语义 + 目录 301，100% 流量 |
| `de2b7d51-1cae-44d5-bb7f-9d78f180edce` | 2026-09-23 02:18Z | 批次 1–6（当时除 B6 真锁/B9 外的全部条目 + F1/F2/F3），100% 流量    |
| `cbb1f962-9a27-40d6-8680-b51f03187615` | 2026-09-23        | 上一个已上线版本，只到 `5b8b9ae`（无 F 组、无上传同名保护）         |
| `71a44db8-c3c5-4f01-ad29-dab8050603c6` | 2026-09-23        | 首次部署                                                            |

上线前回归全绿，并用 `wrangler deploy --dry-run --outdir` 校验**上传包**（与实际上传体积一致）
含全部修复标记。回滚：`npx wrangler rollback -y`。

**刻意保留、本轮不改的条目（全量）**

> B6 与 B9 已在**批次 7** 修掉（复核抓出来的那两条），下面不再列。剩下的全都是**刻意保留**的：
> 有意偏离、成本优化、或者需要单独一次变更才能验证。

| ID  | 情况                                      | 为什么留着                                                                                                   |
| --- | ----------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| C8  | MKCOL 带 body 仍返 201（应 415）          | 有意偏离，保留并记录（收紧只会误伤那些习惯带空 body 的客户端）                                               |
| D5  | DELETE 大树仍严格串行                     | 实测 1100 成员 267 ms，可接受，不引入并发复杂度                                                              |
| D8  | 仍恒带 `include: [...]` 且挂 `@ts-ignore` | 只是多读元数据，无正确性影响；改动需先验证 Miniflare 与生产的类型差异                                        |
| D9  | `compatibility_date = 2023-10-16` 未动    | 改它会一次性打开两年来的运行时行为变更，必须单独一个变更、单独验证                                           |
| E2  | 目录仍是 0 字节标记对象                   | 架构特性：外部工具看到的就是“与文件夹同名的空文件”，改为其它方案（如仅靠前缀）反而会丢目录语义；已在文档记录 |
| E3  | 无分片上传                                | 平台限制（请求体 100 MB），大文件需走 S3 兼容 API                                                            |
| E5  | 未配 `storageClass`                       | 成本优化项，不影响正确性                                                                                     |

> **B4/B5/B8 的设计取舍（需要你确认的一个行为变化）**
> PROPPATCH 从"假装成功"改成**如实回 403**：RFC 4918 §9.2 要求服务器不设置的属性必须
> 逐个回 403，且整个请求必须原子地失败。不实现"死属性"是刻意决定：
> R2 binding **没有"只改元数据"的接口**，要存属性必须把整个对象重传一遍（自定义元数据
> 还有 2 KiB 上限），而 Finder / Windows 每次上传都会发 PROPPATCH —— 代价与收益完全
> 不成比例。改动同时去掉了那次无意义的重传（B8）。
> 风险：若某个客户端把 PROPPATCH 失败当上传失败，可用一行改回"接受并忽略"
> （把 `403 Forbidden` 换成 `200 OK`）。**建议你在 Finder 里实测一次上传。**

> **C12 的结论：不实现配额，而不是编造数值。** 计算已用空间需要遍历整个 bucket
> （子请求爆炸），而 RFC 4331 规定服务器不支持配额时**不应**返回这两个属性 ——
> 返回 0 反而会让客户端以为磁盘已满。现在显式请求它们会得到 `404 propstat`（Batch 4
> 的收敛逻辑顺带实现了这一点），`allprop` 里也不会出现。

> **B10 的迁移影响**：现在 `make_resource_path()` 会解码（`decode_path`），href 会重新编码
> （`encode_path`），两者互逆。存量对象仍然可达 —— 旧版本把 `old name.txt` 存成了 key
> `old%20name.txt`，新版本会输出 `/old%2520name.txt`，客户端请求回来解码后正好命中旧 key
> （已实测）。唯一的代价：这类**存量**对象在各客户端里显示的名字会变成 `old%20name.txt`
> （以前显示 `old name.txt`）；新写入的文件名从此是正确的。
> 另外 `Destination` 头、网页界面的 `directory_path()` 也必须同步解码，否则 COPY/MOVE
> 的目标名会写成编码形式（第一次就漏了 Destination，被回归抓到）。

> **A3/A4 的残余限制（平台相关）**：修掉的是"静默"，不是"上限"。单次请求仍然最多枚举
> `MAX_OBJECTS_PER_REQUEST`(3000) 个成员；现在超限会明确失败（COPY/MOVE → `507`，
> PROPFIND → `<responsedescription>` + `X-WebDAV-Truncated: true`，网页界面 → 顶部警示条），
> 而不是假装成功。真正的分页需要 WebDAV 之外的游标机制，见下方平台受限表。

> **B3 的修法**：没有让 PUT 层层补建祖先目录（那会给每次深层 PUT 增加多次子请求），
> 而是新增 `listDir()` 读取 R2 的 `delimitedPrefixes` —— 隐式目录在任意列表里都能出现，
> 且 `resource_exists()` 让 PROPFIND / DELETE 也能识别它们。存量幻影目录因此一并被救活。

> **A6 说明**：本地观察到的 `500` 是 Miniflare 的 R2 条件读取实现缺陷（错误页内容为
> `Error get: Unspecified error (0)`），生产 R2 未必复现。但代码层面确实存在两处真实缺陷且已修：
> ① 条件不满足时一律回 `412`，而 RFC 9110 要求 GET/HEAD 的 `If-None-Match` /
> `If-Modified-Since` 回 `304`；② 条件判断完全委托给 R2 的 `onlyIf`，无法区分这两种结果。
> 现在改为 `evaluate_conditionals()` 自行按 §13.2.2 的优先级求值（1 次请求，无额外子请求）。

> **C2/C1 附带教训**：不能用 `object.range !== undefined` 判断"是否返回了片段" ——
> R2 在请求**没有** Range 头时也会返回覆盖整个对象的 range，那样会让所有普通下载都变成
> `206 + Content-Range`（我第一版就这么写错了，被回归抓到）。必须用长度比对。

> **复核时我把一条判断写错了（已更正）**：B6 里“LOCK 不存在的资源”我当时凭直觉写成“应 404”。
> RFC 4918 §9.10.4 与 §9.10.6 的原文是：对未映射 URL 的 LOCK 成功时**必须创建一个空的、
> 非集合的资源**并回 **201 Created**（附录 D 明确废弃了旧的 lock-null resource 模型）。
> 实现与测试现在都按 RFC 的 201 做。教训：状态码这类细节必须回原文，不能凭印象填。

> **B6 的锁状态存在哪里（批次 7）**：Workers 隔离环境没有跨请求共享内存，锁必须落到共享
> 存储才有效；而 R2 没有“只改元数据”的接口（B8），所以锁不能塞进被锁对象本身。最终存在
> **一个旁路对象** `._locks` 里：`.` 开头本来就被 `is_os_metadata_key()` 过滤，不会污染任何
> 列表；单个 key 又不含 `/`，不会产生 `delimitedPrefixes`（看不到幽灵目录）。读取时顺带校验：
> 过期的、以及锁根已不存在的记录会被丢掉，所以不必去改 DELETE/MOVE 那几条热路径
> （RFC 4918 §6.1 第 8 点要求锁根变成未映射 URL 时锁必须跟着消失）。
>
> **B6 仍不强制**：PUT / DELETE / MOVE 不检查锁令牌。这**不是**平台限制 —— 技术上现在可以做了
> （状态已经持久化，只需多一次读 + 解析 `If` 头）。没做的原因是：一旦 `If` 头解析出错，
> Office / Finder 的保存会直接以 423 失败，那是**数据写不进去**的风险，必须先用真实客户端
> 逐条验证（先当只读观察者记录它们到底发什么 `If`，再开启强制）。在那之前**锁是 advisory 的**。

---

## A. P0 — 数据丢失 / 完全不可用

| ID  | 问题                                                                                     | 证据                                     | 状态                       |
| --- | ---------------------------------------------------------------------------------------- | ---------------------------------------- | -------------------------- |
| A1  | `DELETE /` 清空整个 bucket，无确认，客户端探测即全量销毁                                 | [实测] 7 条 → 0 条，数据 DESTROYED       | **已修复**（改为 403）     |
| A2  | `bucket.put()` 条件失败返回 `null` 被忽略，谎报 `201`；R2 实际拒绝写入                   | [实测] `If-None-Match:*` → 201，内容未变 | **已修复**（改为 412）     |
| A3  | COPY/MOVE 超过 3000 成员时静默截断，回 `201`，其余成员丢失/变孤儿                        | [实测] 3100 → 只处理 3000                | **已修复**（改为 507）     |
| A4  | 列表（PROPFIND 与 Web UI）静默截断，无 507、无游标，超出部分对所有客户端不存在           | [实测] 3001 条 / f03100.txt 缺失         | **已修复**（改为显式失败） |
| A5  | `generate_propfind_response` 未转义，含 `&`/`<` 的文件名产出非法 XML，整目录列表不可解析 | [实测] xmllint parser error              | **已修复**（新增转义）     |
| A6  | 条件 GET（`If-None-Match` 命中）返回 `500` 而非 `304`                                    | [实测] 500                               | **已修复**                 |

## B. P1 — 互操作与数据完整性

| ID  | 问题                                                                                                                                                         | 证据                                             | 状态                                                 |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ | ---------------------------------------------------- |
| B1  | MOVE 的 `Overwrite` 默认值反了（应为 `T`，RFC 4918 §10.6），与 COPY 自相矛盾                                                                                 | [实测] no-header → 412                           | **已修复**                                           |
| B2  | MOVE 集合 + `Depth: 0` 应 400（§9.9.3），实际搬走标记、留下孤儿                                                                                              | [实测] 201 + 孤儿                                | **已修复**                                           |
| B3  | PUT 只创建末级父目录，祖先成为"幻影目录"，列表不可见、按名删不掉（根因：从不读 `delimitedPrefixes`）                                                         | [实测] `/t7/` 缺失、`DELETE /t7` 404             | **已修复**                                           |
| B4  | PROPPATCH 完全空转却报 207（`HTMLRewriter` 只注册在根元素，收集逻辑永不触发）                                                                                | [实测] 空 `<multistatus>`，0 个 `<response>`     | **已修复**                                           |
| B5  | PROPPATCH 无法设置受保护属性（应 403），属性名被小写化（XML 大小写敏感）                                                                                     | [实测] 报 200                                    | **已修复**                                           |
| B6  | LOCK/UNLOCK 语义缺失：重复互斥 LOCK → 200（应 423）、不存在的资源 → 200、缺 `<lockroot>`、忽略 `Timeout`；假 token → 204（应 409）；无 token → 204（应 400） | [实测] 全部确认                                  | **已修复**（协议语义；写锁 enforcement 仍 deferred） |
| B7  | PUT 静默忽略 `Content-Range`，分块上传的后一块整体替换前一块 → 静默截断文件                                                                                  | [实测] 11 字节文件变 6 字节                      | **已修复**（改为 501）                               |
| B8  | PROPPATCH 为重写元数据而重传整个对象（get+put 全量字节），且非原子                                                                                           | [代码]                                           | **已修复**                                           |
| B9  | 目录 GET（无尾斜杠）返回 200 + 空体，应 301 重定向                                                                                                           | [实测] 200 (no Location)                         | **已修复**（改为 301）                               |
| B10 | 键按百分比编码存储（路径从不解码），与 R2 控制台/S3 API/`wrangler r2`/rclone S3 remote 名字不一致                                                            | [实测] `my%20file.txt`、`%E4%B8%AD%E6%96%87.txt` | **已修复**                                           |

## C. P2 — 语义与细节

| ID  | 问题                                                                                      | 证据                   | 状态                                    |
| --- | ----------------------------------------------------------------------------------------- | ---------------------- | --------------------------------------- |
| C1  | 200 响应上带 `Content-Range`（RFC 9110 §15.3.7 禁止）                                     | [实测] `bytes 0-10/11` | **已修复**                              |
| C2  | 空对象返回 `Content-Range: bytes 0--1/0`（非法负区间）                                    | [实测]                 | **已修复**                              |
| C3  | 缺 `Accept-Ranges: bytes` → 客户端不做范围请求（视频拖动、PDF 分页）                      | [实测] 缺失            | **已修复**                              |
| C4  | GET/HEAD 无 `ETag` / `Last-Modified` → 无缓存再验证通路                                   | [实测] 缺失            | **已修复**                              |
| C5  | `getetag` 未加引号（用 `etag` 而非 `httpEtag`）                                           | [实测]                 | **已修复**                              |
| C6  | PROPFIND 完全忽略请求体：`<propname/>` 返回真值、`<prop>` 不收敛、未知属性无 404 propstat | [实测]                 | **已修复**                              |
| C7  | 非法 `Depth` 返回 403（§10.2 要求 400）                                                   | [实测] 403             | **已修复**（改为 400）                  |
| C8  | MKCOL 带 body 创建成功（应 415）                                                          | [实测] 201             | 有意偏离，保留并记录                    |
| C9  | PUT/MKCOL 进入"父为文件"的路径成功（应 409），同一 key 既是文件又是文件夹                 | [实测] 201             | **已修复**（改为 409）                  |
| C10 | `Destination` 指向外部主机时照写本桶（应 502）                                            | [实测] 201             | **已修复**（改为 502）                  |
| C11 | COPY/MOVE 的 `destination_parent` 计算里 `endsWith('/')` 是死分支                         | [代码]                 | **已修复**（已清理）                    |
| C12 | 缺 RFC 4331 `quota-available-bytes` / `quota-used-bytes`                                  | [实测]                 | **已修复**（不实现，显式 404 propstat） |
| C13 | `displayname` 取自 `Content-Disposition`；PROPPATCH 永远无法设置它                        | [代码]                 | **已修复**                              |
| C14 | MKCOL 把请求头当 `httpMetadata` 写进目录标记                                              | [代码]                 | **已修复**（不再写入）                  |

## D. 性能 / 阻塞

| ID  | 问题                                                                                                                    | 证据                    | 状态                                 |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------ |
| D1  | COPY/MOVE 每成员 2–3 次 R2 调用；R2 调用计入**子请求**配额（Free 1,000 / Paid 10,000）→ 超限即 500 且**非原子、无回滚** | [推导]                  | **已缓解**（507）· 彻底方案平台受限  |
| D2  | `MAX_CONCURRENT_OPERATIONS = 50` 不可达：平台限制**每次调用 6 个并发连接**，多余排队；按 50 分批还引入队头阻塞          | [推导]                  | **已修复**（50 → 6）                 |
| D3  | PROPFIND 无界开销：`Depth` 缺省即 `infinity`，实测单个目录 2002 KiB / 66–208 ms；Free 计划 CPU 仅 10 ms                 | [实测]+[推导]           | **已缓解**（按请求体收敛）· 平台受限 |
| D4  | `processWithConcurrencyLimit` 里 `results` 数组是死代码                                                                 | [代码]                  | **已修复**（已删）                   |
| D5  | DELETE 大树严格串行（list 分页 + 逐页 delete）                                                                          | [实测] 1100 成员 267 ms | 可接受，记录                         |
| D6  | `MAX_BATCH_DELETE_SIZE = 3000` 与 `delete()` "每次最多 1000 键" 冲突                                                    | [推导] 当前不可达       | **已修复**（改为 1000 + 注释）       |
| D7  | `MAX_PROPFIND_DEPTH = 5` 声明但从未使用                                                                                 | [代码]                  | **已修复**（已删）                   |
| D8  | 恒定 `include: ['httpMetadata','customMetadata']` 且挂 `@ts-ignore`                                                     | [推导]                  | 记录                                 |
| D9  | `compatibility_date = 2023-10-16` 偏旧                                                                                  | [文档]                  | 独立任务                             |

## E. R2 存储层

| ID  | 问题                                                                                                            | 证据   | 状态                                                          |
| --- | --------------------------------------------------------------------------------------------------------------- | ------ | ------------------------------------------------------------- |
| E1  | 键被百分比编码写入（同 B10）                                                                                    | [实测] | **已修复**（随 B10）                                          |
| E2  | 目录 = 0 字节目录标记对象；对非本 Worker 的工具就是一个"与文件夹同名的空文件"；标记缺失即幻影目录               | [实测] | **已修复**（幻影目录随 B3）；0 字节标记为架构特性，保留并记录 |
| E3  | 未使用 R2 分片上传，也无 WebDAV 分片/续传；上传受 Cloudflare 请求体上限约束（Free/Pro 100 MB，Business 200 MB） | [文档] | **平台受限**                                                  |
| E4  | 全站无游标分页                                                                                                  | [代码] | **已修复**（随 A4，改为显式失败）                             |
| E5  | 无 `storageClass` 配置（未用 IA 存储类）                                                                        | [代码] | 记录（成本优化）                                              |

---

## F. 网页界面（修复过程中新发现）

| ID  | 问题                                                                                                                                                           | 证据                                                 | 状态   |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------- | ------ |
| F1  | 拖拽/选择上传同名文件时**静默覆盖**（无提示、无确认），只在事后弹一句“已上传 N 个文件”                                                                         | [实测] 内容 `ORIGINAL-CONTENT` → `REPLACED`          | 已修复 |
| F2  | “新建文本文件”默认名恒为 `untitled.txt`：只要该名已存在，新建流程一打开就处于重名风险中，连点两次保存即覆盖原有文件                                            | [实测] 默认名 `untitled.txt`                         | 已修复 |
| F3  | **P0**：编辑器的 `conflict` 标记一旦置位就永不重置 —— 改名到**另一个**已存在的文件后首次保存会**直接覆盖**它（警告文字还是上一个文件名留下的，看起来像已确认） | [实测] `second.txt`：`ORIGINAL-SECOND` → `CONTENT-A` | 已修复 |

> **F3 为什么是 P0**：它不是“提示不到位”，而是**探测被彻底跳过**。
> 旧逻辑 `if (!editor.href && !editor.conflict)` —— 第一次撞名后 `conflict = true`，
> 之后无论改成什么名字都不再发 `HEAD` 探测，直接 `PUT`。
> 修法是让 `conflict` 跟随文件名复位（输入框上 `@input="editor.conflict = false"`），
> 于是“再点一次保存就覆盖”这句话**只对当前这个名字成立**，无法通过改名绕过。
>
> **F2 的修法**：新建时用 `default_new_name()` 在已加载的 `entries` 里挑一个不冲突的名字
> （`untitled.txt` → `untitled-2.txt` → …），**不发额外请求**；同时 `untitled.txt` 已存在
> 这件事不再把用户直接推到“重名 - 再点一次 - 覆盖”的边上。
>
> **F1 的修法**：上传时发 `If-None-Match: *`，`412` 不当作失败而是收集起来，全部传完后再弹
> 一张确认表（列出文件名，超过 5 个折叠为“……以及另外 N 个”），确认后才重跑一遍覆盖。
> 同名**目录**单独识别（缓存条目 + 一次带 `<collection />` 的 PROPFIND 交叉校验，防列表过期），
> 归入“跳过同名目录”而不是覆盖。

---

## 平台受限项（记录后跳过）

这些**无法在 Cloudflare Workers 上按要求完成**，不在本轮修复范围内，仅记录原因与替代方案：

| 项                                            | 平台原因                                                                                                                             | 替代方案                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **B6 真锁**（互斥锁、423 Locked、token 校验） | Workers 是无跨请求共享内存的隔离运行环境；锁必须持久化到共享存储才有效。真正的**强制**锁需要 **Durable Objects**（或 KV + 竞态处理） | **已修协议语义**：锁状态存在一个旁路 R2 对象（`._locks`）里，所以 423 / 409 / 400 / 412 / 201、`<lockroot>`、`Timeout` 全对了，PROPFIND 也能报出真实 lockdiscovery。⚠️ 仍**advisory**：PUT/DELETE 不要求提交锁令牌（一旦 `If` 头解析出错，Office/Finder 的保存会直接失败，属必须单独验证的一步）。彻底强制锁 + 无竞态仍需 DO 绑定 |
| **B8 只改元数据的更新**                       | R2 Workers binding **没有** updateMetadata / copy-with-replace 接口：要改 `customMetadata` 就必须重新 `put` 整个对象                 | 已改为"零写入 + 如实回 403"（不再做那次重传）。若将来要支持死属性，需要旁路对象（sidecar）或 DO                                                                                                                                                                                                                                   |
| **E3 大文件上传 / 分块续传**                  | Cloudflare 请求体上限 100 MB（Free/Pro）/ 200 MB（Business），且 Worker 无法绕过；WebDAV 本身没有标准的分片上传                      | 已修 B7：对 `Content-Range` 明确回 `501`，**不再静默产出截断文件**；大文件需走 S3 兼容 API（`createMultipartUpload`）而非 WebDAV                                                                                                                                                                                                  |
| **A4 列表分页**                               | 单次响应可枚举的成员数受 CPU（Free 仅 10 ms）与子请求配额限制；WebDAV 没有标准的分页游标，Worker 内也无法跨请求保存遍历状态          | 已改为**显式失败**而非静默截断（COPY/MOVE → 507；PROPFIND → `<responsedescription>` + `X-WebDAV-Truncated`；网页界面 → 警示条）。彻底方案需 Queues / DO 承载可续的任务状态                                                                                                                                                        |
| **D1 子请求上限下的"一次性"COPY/MOVE**        | Free 1,000 / Paid 10,000 子请求，超出即终止（COPY 每成员 2 次 R2 调用、MOVE 3 次）                                                   | 已改为"有界 + 明确失败"（507），不再静默丢数据。彻底方案同上                                                                                                                                                                                                                                                                      |
| **D3 PROPFIND 的 CPU 开销**                   | 平台 CPU 配额；代码只能在预算内减少工作量                                                                                            | 现在支持按请求体收敛属性（`<prop>` / `<propname/>`），客户端可以只要它需要的字段；Paid 计划可调 `limits.cpu_ms`                                                                                                                                                                                                                   |
| **D2 6 连接并发**                             | 平台每次调用最多 6 个等待响应头的连接                                                                                                | 已把常量从 50 改成 6，并注明原因，避免"50 路并发"的错觉                                                                                                                                                                                                                                                                           |

---

## 修复批次计划

| 批次 | 内容                                                                 | 风险                     |
| ---- | -------------------------------------------------------------------- | ------------------------ |
| 1    | A1 A2 A5 B1 B2 B7 C1 C2 C3 C4 C5 C7 C9 C10                           | 低：局部条件判断与响应头 |
| 2    | A3 A4 B3（列表/枚举语义：显式失败 + `delimitedPrefixes` + 幻影目录） | 中：改动列表与遍历       |
| 3    | B10（路径解码 + href 编码，涉及存量键命名迁移）                      | 中：影响既有对象的显示名 |
| 4    | A6 B4 B5 B8 C6（条件 GET、PROPPATCH、PROPFIND 请求体）               | 中：需要协议细节回归     |
| 5    | C11 C12 C13 C14 D2 D4 D6 D7（清理与补齐）                            | 低                       |
| 6    | F1 F2 F3（上传/新建的同名保护，纯网页界面层）                        | 低：不改服务端协议       |
